#!/usr/bin/env python3
"""
Experimental prompts for a dedicated step3 naturalization/diversification pass.
"""

STEP3_EXP_SYSTEM_PROMPT = (
    "你负责把 step2 产出的 query 再改写一轮，让它更自然、更多样、也更像真人真的会发给 AI 的复杂请求。"
    "这一步只做自然化和去模板化，不允许改任务边界。"
    "必须保留：主目标、关键限制、主交付物、地点约束、本地材料前提、必要的具体复核点，以及同一任务下不同结果层各自的用途。"
    "不要新增新的主任务，不要新增 skill 明显不支持的要求，不要无端增加交付物。"
    "不要把 query 改短成一句空话，也不要继续扩成说明书。"
    "如果要吸收 persona 信息，必须把它自然融进任务动机、使用场景、决策压力或结果用途里，不要像贴标签一样单独挂一句背景。"
    "如果 query 里同时有主结果、决策版、执行版或转发版，不要把它们压扁成一句模糊的“再整理一版”；要保留它们各自存在的理由。"
    "如果样本标记为 user_files_required，必须保留任务建立在用户手头已有文件、截图、原始材料或底稿之上的前提。"
    "允许用户说得不像流程说明书，但要让人明显感觉到执行智能体还需要自己补中间判断、筛选和推进动作。"
    "如果必须保留复核或准确性要求，优先改写成用户真实会担心的后果，例如怕白跑、怕买错、怕发出去被质疑、怕回头还得自己重查。"
    "不要老是出现“核对、对得上、口径、一致”这种审校腔词组，除非不写就会丢掉任务边界。"
    "明确避免出现这些过度模板化的话术或近似说法：‘白忙一场’、‘没法交代’、‘千万别出岔子’、‘还得自己重查一遍’、‘别让我回头再去查’。"
    "输出只是一段中文用户消息，不要解释，不要列点，不要加引号。"
)


STEP3_EXP_USER_PROMPT_TEMPLATE = """你要把下面这条 step2 query 再自然化一轮。

目标：
- 更像真人真实发给 AI 的消息
- 语言更松弛、更自然，但仍然复杂
- 去掉模板味、口号味、AI腔
- 允许轻微重组句子，但不能改题
- 保留复杂度，不要把任务压扁
- 让 persona 和任务自然融合，最好写成“为什么这件事对我重要 / 为什么我会这样要求”

硬约束：
- 不改变任务边界
- 不新增新的主交付物
- 不新增新的主任务
- 如果 query 已经明确写了地点、本地材料、关键数字、具体复核点，这些不能丢
- 如果 input_context = user_files_required，必须保留“基于我手头已有材料/文件/截图/原始信息来做”的前提
- 如果 query 里有多个交付物，只保留当前已有的，不要再扩
- 下面列出的 must_keep 信息必须保留，可以换更自然的说法，但不能删掉、更不能弱化成泛话
- 如果 query 里已有“主结果 + 决策版 + 执行/转发版”这种层级，不要压缩成只剩主结果

风格变体：
{style_instruction}

输入信息：

persona_hint:
{persona_hint}

topic:
{topic}

input_context:
{input_context}

must_keep_input_material_hint:
{must_keep_input_material_hint}

secondary_chain_kind:
{secondary_chain_kind}

must_keep_relative_paths:
{must_keep_relative_paths}

must_keep_locations:
{must_keep_locations}

must_keep_review_points:
{must_keep_review_points}

current_query:
{current_query}

改写要求：
- 最终仍然是一段用户消息
- 可以是 2 到 4 句
- 尽量像用户在聊天窗口里自然补充需求，而不是正式文档
- 不要机械重复“核对、一致、口径、对不上”这类词
- 如果必须保留复核要求，尽量说得具体、自然
- 不要生硬地把 persona 信息单独挂出来，例如“我是XX，所以……”
- 更推荐把 persona 融进任务原因、使用场景、决策焦虑、结果用途，例如“我自己拿不准”“我要发给对象一起商量”“我怕漏材料白跑一趟”
- 如果必须保留准确性要求，尽量改写成更像真人会说的话，比如“别让我白跑一趟”“别害我买错”“别让我回头还得自己重新查一遍”
- 句式尽量有变化，不要都写成“帮我……最后给我……另外再……”
- 如果 query 里有决策版、执行版、转发版，尽量写清它们分别是给谁看、拿来干什么
- 可以把一部分中间流程藏起来，但不能把“需要执行智能体自行补推理”的那种厚度改没
- 如果 must_keep_relative_paths 不为空，这些路径必须原样保留
- 如果 must_keep_input_material_hint 不为空，必须保留这类文件/材料前提，不能弱化成普通公开检索任务
- 如果 must_keep_locations 不为空，这些地点必须明确保留，不能改成泛化地区
- 如果 must_keep_review_points 不为空，必须保留这些具体复核点，不能改成泛泛的“帮我核一下”
- 明确不要出现这些套话或很像它们的句子：‘白忙一场’、‘没法交代’、‘千万别出岔子’、‘还得自己重查一遍’、‘别让我回头再去查’

现在直接输出改写后的用户消息。"""
