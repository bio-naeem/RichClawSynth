#!/usr/bin/env python3
"""
Experimental prompts for a richer step2 rewrite pass under gpt-exp/.
"""

STEP2_EXP_SYSTEM_PROMPT = (
    "你负责把 step1 的 hidden_plan 改写成更像真人会发给 AI 的中文消息。"
    "这次不要把任务压得过短，也不要只保留一个很泛的目标。"
    "你的目标是保留任务复杂度、关键约束、主要交付和真实使用场景，同时去掉机器味和说明书味。"
    "输出必须像真实用户消息，但信息密度要明显高于普通一句话提问。"
    "优先保留：核心目标、关键限制、明确交付物、重要筛选标准、时间/地点/对象、必须保留的相对路径。"
    "可以弱化：默认中间动作、实现细节、过度结构化的执行步骤。"
    "要让执行任务的智能体承担更多隐含工作，用户消息里可以只说目标、标准、交付和少量关键限制，不要把中间步骤全部说破。"
    "如果任务需要中间判断、整理、筛选、核对、比对、取舍或组装过程，尽量把这些工作留给执行智能体自行推理完成。"
    "不要把 hidden_plan 逐条翻译成执行清单。"
    "不要为了口语化而丢失任务边界。"
    "不要新增 step1 里没有的事实。"
    "如果文件格式或结果形式是关键要求，要保留；如果只是实现手段，可以改写成用户使用场景。"
    "如果样本里存在增强链、全局现实约束、多层交付物，要把它们自然保留下来，让任务显得更厚，但仍然围绕单一目标。"
    "如果任务明显依赖用户手头的本地文件、已有材料、现成文档或原始信息，要自然提示这一点，不要假设执行智能体自己不会去找。"
    "如果样本标记为 user_files_required，必须保留“基于我手头这些文件/截图/原始材料来做”的前提，不能改写成纯公开信息查询。"
    "如果保留复核、核查、校对、一致性检查这类要求，必须明确写出复核点，不能只写泛泛的“帮我核一下”或“确保没问题”。"
    "如果样本里存在多层交付，优先保留成“主结果 + 决策适配层 + 执行/转发适配层”这种更深的结构，而不是把它们压成一句附带要求。"
    "允许用户消息里不说完整流程，但要让人明显感觉到：执行智能体需要自行补中间判断、筛选、取舍、组装和推进顺序。"
    "避免反复使用“互相打架”这类固定口头表达，改成更自然的约束说法。"
    "最终输出只是一段用户消息，不要解释，不要列点，不要加引号。"
)


STEP2_EXP_RICH_USER_PROMPT_TEMPLATE = """你要把下面这条 step1 样本改写成更自然、但仍然保留复杂度的中文用户消息。

你会看到：
- persona / topic
- hidden_plan
- 少量高层结构信息
- 全局现实约束
- 多层交付物设计
- 必须保留的相对路径
- 可选的补充需求候选

你的改写目标：
- 像真人会发给 AI 的消息，不能像流程说明书
- 但不要改得太简单，必须保留足够的任务约束和结果要求
- 最终消息建议写成 3 到 5 句，允许自然停顿，但不要分点
- 尽量把大量中间过程藏在请求背后，让执行智能体自己判断先做什么、补什么、核什么、怎么组织结果
- 可以补充 1 到 2 条合理需求，但只能从候选里优先选，且不能改主任务边界
- 这一轮优先保留复杂度和任务密度，宁可稍微更详细，也不要压得太短
- 如果样本里有增强链或多层交付物，要让用户消息看起来像“同一件事要给不同使用场景服务”，而不是机械追加第二任务
- 如果样本里有三层结果，优先写成“主稿负责完整判断，另给决策版，再给执行版/转发版”的自然结构
- 尽量保留 2 到 4 个最关键的信息块：
  1. 用户真正要完成什么
  2. 结果要长什么样 / 给谁用 / 怎么用
  3. 最重要的限制、筛选条件或风险提醒
  4. 如果有路径、硬性交付格式或明确对象，要保留

哪些信息通常该保留：
- 核心任务目标
- 任务对象、范围、地点、时间、受众、用途
- 最关键的判断标准、对比要求、风险提醒、准确性要求
- 明确的主交付物
- 真的影响任务边界的附加结果
- 本地相对路径
- 如果任务本来就建立在“我手头已有文件/材料/原始信息”之上，要把这一前提自然写出来

哪些信息通常不必展开：
- 默认中间动作，例如检索、识别、提取、转格式、汇总
- 只是为了吸收 skill 而扩出来的包装动作
- 不影响任务边界的复核、润色、展示增强
- 那些本应由执行智能体自行推理出来的中间状态与衔接动作

额外要求：
- 不要把每个 support skill 都翻译进最终用户消息
- 如果有多个附加交付，只保留最影响任务边界的 1 到 2 个
- 用更自然的方式表达准确性要求，避免重复出现同一种套话
- 让人读完后感觉“这件事要做很多隐含工作”，而不是“用户已经把步骤写完了”
- 如果有 2 到 3 层交付物，尽量写清它们各自用途，不要只是堆文件格式名
- 如果有“决策版”和“执行版/转发版”，尽量同时保留，不要合并成一个模糊的“再整理一版”
- 如果有全局约束，优先吸收那些真的会改变判断标准、输出形式或执行顺序的约束
- 如果任务依赖本地文件、已有材料、原始信息或现成文档，可以明确说“基于我手头这些材料/文件来做”
- 如果出现复核、校对、核验、一致性检查，必须具体到要核什么，例如数字、日期、名单、价格、材料项、版本差异、结论口径
- 如果 secondary_chain 更像“决策整理”或“转发版整理”，优先写清不同交付物各自的用途，不要默认补一条“它们彼此必须完全一致”
- 补充需求默认优先级：decision_constraint > usage_context > review_point
- review_point 只有在策略明确允许时才能用，而且必须具体到检查点
- 尽量只基于 hidden_plan 和少量高层结构信息改写，不要脑补 skill 层面的包装动作
- 可以适当把“用户不会完整说出的中间任务”藏进表达里，例如让执行智能体自己去拆评估维度、推进顺序、取舍门槛和备用方案，但不要明写成流水账

如何判断“不能改太简单”：
- 改写后仍应让人看得出这是个复杂请求，而不是一个空泛主题
- 不能只剩“帮我看看/整理一下/分析一下”
- 至少要保留结果导向 + 1到2个关键限定

输入如下：

persona_hint:
{persona_hint}

topic:
{topic}

input_context:
{input_context}

input_material_hint:
{input_material_hint}

scenario:
template:
{template}

template_description:
{template_description}

main_delivery_hint:
{main_delivery_hint}

secondary_chain_kind:
{secondary_chain_kind}

enhancement_chain_kind:
{enhancement_chain_kind}

global_constraints:
{global_constraints}

delivery_layers:
{delivery_layers}

secondary_chain_guidance:
{secondary_chain_guidance}

review_policy:
{review_policy}

location_policy:
{location_policy}

persona_usage_policy:
{persona_usage_policy}

extra_requirement_policy:
{extra_requirement_policy}

extra_requirement_candidates:
{extra_requirement_candidates}

must_keep_relative_paths:
{relative_paths_block}

hidden_plan:
{hidden_plan_block}

现在直接输出改写后的用户消息。"""
